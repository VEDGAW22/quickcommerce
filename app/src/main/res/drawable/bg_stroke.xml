<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/transparent"/> <!-- Transparent fill -->
    <stroke
        android:color="@color/black"
         android:width="2dp" />
    <size
        android:width="100dp"
        android:height="100dp"/> <!-- Size of the circle -->
    <corners android:radius="50dp"/> <!-- Circular corners to create a circle -->
</shape>